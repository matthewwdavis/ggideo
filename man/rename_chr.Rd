% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename_chr.R
\name{rename_chr}
\alias{rename_chr}
\title{Rename chromosomes by extracting numeric part}
\usage{
rename_chr(data)
}
\arguments{
\item{data}{A data frame containing a column named \code{Chromosome} that lists chromosome names. The extraction is based on the numeric part of the chromosome name.}
}
\value{
A data frame with the \code{Chromosome} column containing only the numeric part of the chromosome names, converted to numeric type.
}
\description{
This function extracts the numeric part of chromosome names in the \code{Chromosome} column of a dataset. It removes any non-numeric
characters and converts the chromosome names into numeric values. This is useful when chromosome names include both letters
and numbers, and only the numeric part is required for analysis.
}
\examples{
# Example usage
data <- data.frame(Chromosome = c("Chr1", "Chr2", "Chr3", "ChrX"),
                   Value = c(100, 200, 300, 400))
rename_chr(data)

}
